VendorMine.directive( 'rooms', 
	[
		function directive( features, dateSetter ){
			return {
				"scope": {},
				"restrict": "A",
				"link": function link( scope, element, attribute ){
					
					scope.$on()
				  
				}
			}
		}
	] )